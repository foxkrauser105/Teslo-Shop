# TesloShop

1. Clone repository
2. Clone file .env.template and rename it to .env
3. Execute command `npm install` to install dependencies
4. Verify backend is running on port 3000
5. Exeucte command `npm run dev` to start dev server